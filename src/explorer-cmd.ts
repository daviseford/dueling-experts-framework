/**
 * `def explorer` — standalone multi-session browser UI.
 * Starts the watcher server without creating a session.
 */
export async function run(_argv: string[]): Promise<void> {
  console.log('Explorer mode is not yet implemented.');
  console.log('Run `def history` to list sessions from the terminal.');
  process.exit(0);
}
